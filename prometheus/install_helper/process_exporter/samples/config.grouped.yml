# Process Exporter 그룹화 수집 설정 샘플 (운영 및 대규모 환경 권장)
#
# 프로세스를 PID 구분 없이 실행파일명(Comm) 기준으로 그룹화하여 수집합니다.
# 동일 프로세스가 재기동되어 PID가 변하더라도 새로운 시계열이 생성되지 않아
# 운영 환경에서 안전하게 수집 지표 개수를 통제할 수 있습니다.

process_names:
  - name: "{{.Comm}}"
    cmdline:
      - '.+'
